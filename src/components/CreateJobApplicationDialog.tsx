import { Dialog } from "./ui/dialog";

interface Props {
  columnId: string;
  boardId: string;
}

const CreateJobApplicationDialog = ({columnId, boardId}: Props) => {
  return (
      <Dialog>
        
      </Dialog>
  );
};

export default CreateJobApplicationDialog;